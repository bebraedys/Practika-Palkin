<Window x:Class="palkinprog.Dialogs.SalesHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:palkinprog.Dialogs"
        mc:Ignorable="d"
        Title="История продаж"
        Height="550" Width="850"
        WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style x:Key="InfoCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#F3F3F3"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#106EBE"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F3F3F3"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#D1D1D1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E0E0E0"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DangerButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#D13438"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#A4262C"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок и информация -->
        <StackPanel Grid.Row="0">
            <Border Style="{StaticResource InfoCardStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0">
                        <TextBlock x:Name="PartnerNameText" Text="Партнер" FontSize="20" FontWeight="Bold" Foreground="#333"/>
                        <TextBlock x:Name="SalesCountText" Text="Всего продаж: 0" FontSize="14" Foreground="#666" Margin="0,4,0,0"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                        <TextBlock Text="Текущая скидка:" FontSize="14" Foreground="#666"/>
                        <TextBlock x:Name="DiscountText" Text="0%" FontSize="24" FontWeight="Bold" Foreground="#0078D4" TextAlignment="Right"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Сводная информация -->
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="#E8F4FD" Padding="16" CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="Общая сумма" FontSize="12" Foreground="#666"/>
                        <TextBlock x:Name="TotalAmountText" Text="0 ₽" FontSize="18" FontWeight="Bold" Foreground="#0078D4" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Background="#E8F8E8" Padding="16" CornerRadius="8" Margin="8,0">
                    <StackPanel>
                        <TextBlock Text="Сумма скидки" FontSize="12" Foreground="#666"/>
                        <TextBlock x:Name="DiscountAmountText" Text="0 ₽" FontSize="18" FontWeight="Bold" Foreground="#107C10" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Background="#FFF4CE" Padding="16" CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="Следующий уровень" FontSize="12" Foreground="#666"/>
                        <TextBlock x:Name="NextLevelText" Text="-" FontSize="14" FontWeight="Bold" Foreground="#856404" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>
        </StackPanel>

        <!-- Таблица продаж -->
        <Border Grid.Row="1" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4">
            <DataGrid x:Name="SalesDataGrid"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      IsReadOnly="True"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column"
                      Background="White"
                      AlternatingRowBackground="#F9F9F9"
                      RowHeight="36"
                      SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                    <DataGridTextColumn Header="Продукция" Binding="{Binding ProductName}" Width="200"/>
                    <DataGridTextColumn Header="Количество" Binding="{Binding Quantity}" Width="80"/>
                    <DataGridTextColumn Header="Дата продажи" Binding="{Binding SaleDate, StringFormat={}{0:dd.MM.yyyy}}" Width="100"/>
                    <DataGridTextColumn Header="Сумма (₽)" Binding="{Binding Amount, StringFormat={}{0:N2}}" Width="120"/>
                </DataGrid.Columns>
                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem x:Name="EditSaleMenuItem" Header="Редактировать" Click="EditSaleClick"/>
                        <MenuItem x:Name="DeleteSaleMenuItem" Header="Удалить" Click="DeleteSaleClick"/>
                    </ContextMenu>
                </DataGrid.ContextMenu>
            </DataGrid>
        </Border>

        <!-- Кнопки управления -->
        <Grid Grid.Row="2" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1" Content="Добавить" Style="{StaticResource PrimaryButtonStyle}"
                    Click="AddSaleClick"/>
            <Button Grid.Column="2" Content="Обновить" Style="{StaticResource SecondaryButtonStyle}"
                    Click="RefreshClick" Margin="8,0"/>
            <Button Grid.Column="3" Content="Закрыть" Style="{StaticResource SecondaryButtonStyle}"
                    Click="CloseClick"/>
        </Grid>
    </Grid>
</Window>
